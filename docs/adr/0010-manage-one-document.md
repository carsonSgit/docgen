# Manage one document in the MVP

The playground manages exactly one persistent Local Document, which may span multiple pages. A document list, file manager, and multiple-document storage are excluded from the MVP.
