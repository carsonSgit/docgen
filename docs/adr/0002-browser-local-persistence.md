# Persist the working document in the browser

The MVP stores its single working document in browser-local storage so it survives refreshes without requiring a backend, login, database, or document-management UI. This preserves the playground's simplicity while preventing accidental loss during experimentation.
