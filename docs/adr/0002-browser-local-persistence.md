# Persist the working document in the browser

The single working document is stored in browser-local storage so refreshes do
not discard experiments without requiring login, a database, or file management.
