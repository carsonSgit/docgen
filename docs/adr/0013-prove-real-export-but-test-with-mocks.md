# Prove real export while isolating automated tests

Credential-free tests use the provider boundary. A separate opt-in integration
or manual check proves authorization, creation, compilation, and link return.
