# Verify the POC with fixtures and browser tests

Unit and contract tests cover schemas, normalization, compilation, and mocked
providers; Playwright covers editor and export flows using deterministic
fixtures.
