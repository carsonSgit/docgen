# Verify the POC with fixtures and browser tests

The MVP will use unit and contract tests for the document schema, normalization, compiler, and mocked Google provider, plus Playwright for editor and export-flow tests. A small fixture suite will provide page screenshots/PDF comparisons after the core flow works; the initial supported browser is desktop Chrome.
