# Defer Python runtime dependencies

Although Python is available for future supporting work, the MVP does not require a Python service or Python runtime to operate. The first POC stays TypeScript-based until a concrete Python responsibility justifies the additional runtime and deployment boundary.
