# Stage editor capabilities around a core slice

The eventual editor may include images, tables, headers and footers, comments, mentions, and collaboration. The first proof of concept implements only structured text, basic formatting, lists, alignment, manual page breaks, and automatic pagination, while preserving boundaries that allow the extended capabilities to be added later.
