# Define the canonical Render Metrics contract

The domain owns normalized point-based typography, spacing, list, page,
section, and media metrics. Browser conversion occurs at the renderer boundary;
layout-affecting features extend the contract and its tests first.
