# Model the title as editable metadata

The document title is stored and edited separately from the document body, with “Untitled document” as the default. Google Export uses this title for the newly created Google Doc instead of inferring a name from the first paragraph or heading.
