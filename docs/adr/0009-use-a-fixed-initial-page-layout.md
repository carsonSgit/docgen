# Use a fixed initial page layout

The first POC uses US Letter pages with one-inch margins and automatic pagination. Page size and margin controls are deferred until the core editing and export path is proven, while the document model should not prevent adding them later.
