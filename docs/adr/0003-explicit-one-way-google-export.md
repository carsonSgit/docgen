# Export explicitly to a new Google Doc

The MVP exports only when the user requests it, authorizes Google at that point, and creates a new Google Doc from the current local document. It does not import, continuously synchronize, or update existing Google Docs, keeping the integration one-way and easy to reason about.
