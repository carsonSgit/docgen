# Use a Vite React SPA for the playground

The editor playground will use a Vite React SPA rather than Next.js. This keeps the client-side playground lightweight; the Google export boundary will be provided by a separate TypeScript API rather than coupling the editor to a full-stack framework.
