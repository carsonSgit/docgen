# Preserve manual page breaks as document content

Manual page breaks are modeled as semantic content rather than a preview-only rendering detail. They affect local pagination and must compile to real page breaks in newly exported Google Docs.
