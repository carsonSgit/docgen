# Reserve a canonical document-node extension boundary

Pagination and native compilation consume the shared Core Editor Slice and
reject unsupported nodes explicitly. Future table support must extend the
domain, measurement, pagination, and compiler together.
