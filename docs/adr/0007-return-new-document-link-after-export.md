# Return a link to each newly exported document

Each successful export creates a new Google Doc in the user's Drive, names it from the local document title, and returns its link. The MVP does not select folders or overwrite prior exports, and exporting never mutates the Local Document.
