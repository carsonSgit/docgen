# Use points as canonical document units

The Local Document stores geometry, typography, spacing, and indentation in
points. CSS-pixel conversion belongs only to browser rendering; Google requests
retain points.
