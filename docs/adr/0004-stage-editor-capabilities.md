# Stage editor capabilities around a core slice

The core slice is structured text, basic formatting, lists, alignment, manual
breaks, and automatic pagination. Extended features are added only behind the
same domain, pagination, and export boundaries.
